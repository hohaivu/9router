# Compatibility entry point. The canonical source-build deployment is compose.yaml.
# Keep this file as a thin include so `docker compose` and `./deploy.sh` use
# exactly the same service, health check, and persistent volume definition.
include:
  - path: compose.yaml
